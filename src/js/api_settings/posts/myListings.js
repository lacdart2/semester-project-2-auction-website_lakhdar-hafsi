/* 
import { read } from "./read.js";
import * as storage from "../../utils/storage.js";



read(findMyListings);
function findMyListings() {


    console.log(storage.getFromStorage("profile"));
    let myListings = [];
    const profileName = storage.getFromStorage("profile").name;
    if (profileName = json.seller.name) {
        myListings.push(json);
        console.log(myListings);
    }

} */