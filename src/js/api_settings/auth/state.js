/* import { getFromStorage } from "../../utils/storage.js";

export const isLoggedIn = () => Boolean(getFromStorage("token"));

export const profile = () => getFromStorage("profile"); 
export const credit = () => getFromStorage("credit");  */